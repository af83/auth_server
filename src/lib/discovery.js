
exports.discover = function(host, service, callback, fallback) {
  /* Gets the URL endpoint corresponding to the service on given host.
   * Call callback(url_endpoint) or fallback(error).
   */
  // TODO: implement it
  callback("http://"+host+"/portable_contacts");
};
